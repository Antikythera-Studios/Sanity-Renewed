package croissantnova.sanitydim.client.render.layer;

/**
 * Holds the sanity threshold past which animals refuse to interact with the player.
 * Real rendering of the layer is added in phase J alongside GeckoLib + overlay support.
 */
public final class Blackout
{
    public static final float THRESHOLD = .87f;

    private Blackout() {}
}
