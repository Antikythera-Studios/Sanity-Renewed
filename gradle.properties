org.gradle.daemon=false
org.gradle.jvmargs=-Xmx3G

# Minecraft
mc_version=1.20.1

# Architectury
architectury_plugin_version=3.4-SNAPSHOT
architectury_loom_version=1.10-SNAPSHOT
architectury_version=9.2.14

# Forge
forge_version=47.4.18

# Fabric
fabric_loader_version=0.16.10
fabric_api_version=0.92.6+1.20.1

# Forge Config API Port (Fuzs) - https://www.curseforge.com/minecraft/mc-mods/forge-config-api-port-fabric
# Used so common config code can target Forge-style ConfigSpec on Fabric.
forge_config_api_port_version=8.0.3

# GeckoLib - animated entity renderers
geckolib_version=4.8.3

# Mod metadata
mod_name=SanityRenewed
# mod_version is the minor.patch portion of the full version. The major component
# is derived from mc_version (e.g. 1.20.1 -> 20). Final version: <mcMinor>.<mod_version>-mc<mc_version>
mod_version=1.3
mod_id=sanity_renewed
mod_group=guivnf.sanity_renewed

enabled_platforms=forge,fabric

# Publishing (mod-publish-plugin)
# Tokens are NOT stored here. They are read from environment variables at publish
# time (see gradle/publishing.gradle and publish-secrets.sh).
mod_publish_version=1.1.0
curseforge_id=1547994
curseforge_slug=sanity-renewed
modrinth_id=Fq25IOS1
